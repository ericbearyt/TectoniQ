flask>=3.0.0
flask-cors>=4.0.0
pdfminer.six>=20221105
PyMuPDF>=1.24.0          # fitz — fast PDF text extraction (Workers 1 & 4)
google-genai>=2.0.0
python-dotenv>=1.0.0
PyPDF2>=3.0.0

# ── Vocabulary-linking engine (Worker 7, USE_CLINICAL_LINKER=1) ──────────
# Requires Python <=3.12 (scispaCy/spaCy do not build on 3.14).
# scispaCy pulls nmslib-metabrainz (maintained nmslib fork) automatically.
# The sci NER model + ~1GB UMLS linker KB download on first use:
#   pip install "https://s3-us-west-2.amazonaws.com/ai2-s2-scispacy/releases/v0.5.4/en_core_sci_sm-0.5.4.tar.gz"
scispacy>=0.6.2
spacy>=3.7,<3.8
medspacy>=1.3.0
